import java.util.Random;

public class CalculaRetangulo {
    public static final int TAM = 10;
     public static Random gerador = new Random();
    public static void main(String[] args) {
        Retangulo r = new Retangulo(TAM, TAM);
        CriaRetangulo(r);
    }       

    public static void CriaRetangulo(Retangulo r) {
        int largura = gerador.nextDouble(TAM);
        int altura = gerador.nextDouble(TAM);
        c[0] = new 
    }
}
